# Python
claude.md
__pycache__/
*.pyc
.venv/
venv/

# Environment files with real secrets — only .env.example* are committed
.env
src/.env
docker/env/.env.postgres

# Alembic — the .ini with a real DB URL is a local copy of alembic.ini.example
src/models/db_schemes/raylab/alembic.ini

# Generated per-client Bucket B/C template modules (claude.md §3.5) — these
# are sync-time output, regenerated wholesale on every run, never hand-authored.
src/stores/llm/templates/clients/*/
src/rag_execution.log
# Editor
.vscode/

