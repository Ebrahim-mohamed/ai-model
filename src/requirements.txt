# Steps 1-5 scope: DB schema/migrations, config, schema registry, OneDrive/MSAL
# store, on-demand sync trigger, dynamic sheet parsing. Later steps append
# their own dependencies here as they are built (sentence-transformers, ...)
# — see claude.md §1.2.
SQLAlchemy==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
pgvector==0.4.0
python-dotenv==1.0.1
pydantic-settings==2.2.1

# Step 3 — OneDrive/MSAL store
msal==1.31.0
requests==2.32.3
cryptography==43.0.3

# Step 4 — API + task queue
fastapi==0.110.2
uvicorn[standard]==0.29.0
celery==5.5.3
redis==6.2.0
kombu==5.5.4
billiard==4.2.1
vine==5.1.0

# Step 5 — dynamic 1NF sheet parsing
pandas==2.2.3
openpyxl==3.1.5

# Step 8 — embedding model shootout. torch MUST be installed from the CPU
# wheel index BEFORE this file (see README's Step 8 "New dependencies"
# section) — plain `pip install torch` on Linux pulls the full CUDA
# dependency bundle (~2GB+ of nvidia-* wheels) even though this dev
# machine's GPU (Quadro M2200, 4GB VRAM) offers no real advantage for
# BGE-M3 and is nowhere near enough for Swan-Large's 7B backbone anyway.
# Install a real CUDA build yourself only if you move this to GPU hardware
# that can actually use it. torch>=2.6 is a hard floor, not a preference —
# transformers==4.57.6 (pulled in by sentence-transformers) refuses to run
# torch.load below that (a CVE-related restriction), confirmed the hard
# way when 2.5.1 raised at runtime during the real Step 8 verification.
sentence-transformers==3.3.1
torch==2.13.0
numpy==2.1.3
