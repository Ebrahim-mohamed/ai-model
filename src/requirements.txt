# Steps 1-3 scope: DB schema/migrations, config, schema registry, OneDrive/MSAL store.
# Later steps append their own dependencies here as they are built (fastapi,
# celery, pandas/openpyxl, sentence-transformers, ...) — see claude.md §1.2.
SQLAlchemy==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
pgvector==0.4.0
python-dotenv==1.0.1
pydantic-settings==2.2.1

# Step 3 — OneDrive/MSAL store
msal==1.31.0
requests==2.32.3
cryptography==43.0.3
